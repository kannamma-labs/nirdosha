# Racing a naive money-transfer ledger, Nirdosha vs. Python

> **Legacy content, not yet migrated (2026-09-20).** Every `.nir` file
> here (`race_probe*.nir`) targets the deleted interpreted-`.nir`
> compiler (`crates/compiler`, removed in favor of the v2 Rust dialect)
> — none of it builds or runs today. `RESULTS.md`/`RESULTS_BOOKING.md`
> are a real, historical record of real runs — kept rather than deleted
> because no v2-dialect equivalent exists yet; porting it is separate,
> not-yet-scheduled follow-up work (named explicitly, alongside
> `examples/isolation_demo/`, as the next migration targets in prior
> session planning).

See [`RESULTS.md`](./RESULTS.md) and [`RESULTS_BOOKING.md`](./RESULTS_BOOKING.md)
for the full write-ups.
