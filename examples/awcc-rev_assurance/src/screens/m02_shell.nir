//! M2.1 Global Navigation Shell — AWCC Revenue Assurance.
//!
//! The real shell is generated by `app_shell_from_toml!` in
//! `m02_dashboards.nir`. This module re-exports the mount function so
//! the server binary can wire it once.

pub use crate::m02_dashboards::mount_app_shell;
