//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    FeedPostRow, feed_post_table, feed_post_cell,
    SettingsRow, settings_table, settings_store,
};

nirdosha_rt::settings_screen! {
    mount: mount_app_settings,
    entity: SettingsRow,
    store: settings_store,
    path: "/settings",
    fields: [ site_name: String, maintenance_mode: bool ],
    access: requires role "Admin",
    guard: { table: settings_table, purpose: "Administration", row_id: "singleton" },
}

nirdosha_rt::communication_feed! {
    mount: mount_team_feed,
    entity: FeedPostRow,
    store: feed_post_cell,
    path: "/feed",
    fields: [ author: String, body: String, ticket_id: String ],
    post_access: requires role "Agent",
    read_access: requires role "Agent",
    refresh_seconds: 5,
    guard: { table: feed_post_table, purpose: "Operations" },
}

