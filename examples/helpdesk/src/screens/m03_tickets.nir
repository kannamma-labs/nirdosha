//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    TicketRow, ticket_table, ticket_store,
};

nirdosha_rt::crud_screens! {
    mount: mount_tickets,
    entity: TicketRow,
    store: ticket_store,
    path: "/tickets",
    fields: [ title: String, priority: String, status: String, body: String ],
    create: requires role "Agent",
    read: requires role "Agent",
    update: requires role "Agent",
    delete: public,
    guard: { table: ticket_table, purpose: "Operations", update_fields: [ status: String, priority: String ], create_fields: [ ticket_id: String, title: String, priority: String, body: String ], },
}

nirdosha_rt::wizard! {
    mount: mount_new_ticket_wizard,
    entity: TicketRow,
    store: ticket_store,
    path: "/tickets/wizard",
    access: requires role "Agent",
    steps: [
        { name: "Basics", fields: [ ticket_id: String, title: String, priority: String ] },
        { name: "Confirm", fields: [ body: String ] },
    ],
    guard: { table: ticket_table, purpose: "Operations" },
}

