[package]
name = "nirdosha-lineage"
version = "0.1.0"
edition = "2021"
authors = ["Nirdosha Contributors"]
description = "RFC 0026: governed lineage over the guard kernel — observation types, collector seam, projection, GraphStore port"
license = "MIT"

[dependencies]
nirdosha-guard-core = { path = "../nirdosha-guard-core" }
nirdosha-audit = { path = "../nirdosha-audit" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
