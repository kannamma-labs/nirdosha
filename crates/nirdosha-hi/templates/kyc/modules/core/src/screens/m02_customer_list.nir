//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    CustomerRow, customer_table, customer_store,
};

nirdosha_rt::crud_screens! {
    mount: mount_customer_list,
    entity: CustomerRow,
    store: customer_store,
    path: "/customers",
    fields: [ customer_id: String, legal_name: String, customer_type: String, risk_rating: String ],
    create: public,
    read: public,
    update: public,
    delete: public,
    guard: { table: customer_table, purpose: "Operations", update_fields: [ legal_name: String, customer_type: String ], create_fields: [ legal_name: String, customer_type: String ], },
}

