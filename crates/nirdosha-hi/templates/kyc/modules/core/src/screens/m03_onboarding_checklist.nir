//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    checklist_template_table,
    CustomerRow, customer_table,
};

pub fn __panel_steps(auth: &nirdosha_rt::Auth, subject_id: &str) -> Result<Vec<serde_json::Value>, String> {
    let rows = checklist_template_table().guarded_snapshot(auth, "Administration").map_err(|e| e.to_string())?;
    let mut out: Vec<serde_json::Value> = Vec::new();
    for row in rows {
        if row.template_id == *subject_id {
            out.push(serde_json::to_value(&row).map_err(|e| e.to_string())?);
        }
    }
    Ok(out)
}

pub fn customer_workspace_label(row: &CustomerRow) -> Vec<(String, String)> {
    vec![
        ("Customer".to_string(), row.customer_id.clone()),
    ]
}

nirdosha_rt::workspace! {
    mount: mount_onboarding_checklist,
    path: "/onboarding/{id}/checklist",
    subject: { table: customer_table, purpose: "Operations", label_fn: customer_workspace_label },
    budget: { max_rows: 500, max_execution_ms: 2000 },
    panels: [
        { need: "steps", title: "Checklist steps", render: "list", source: __panel_steps },
    ],
}

