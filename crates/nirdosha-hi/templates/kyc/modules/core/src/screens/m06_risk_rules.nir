//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    RiskRuleRow, risk_rule_table, risk_rule_store,
};

nirdosha_rt::settings_screen! {
    mount: mount_risk_rules,
    entity: RiskRuleRow,
    store: risk_rule_store,
    path: "/risk/rules",
    fields: [ rule_id: String, rule_name: String, severity: String, expression: String ],
    access: requires role "platform_admin" or role "compliance_officer",
    guard: { table: risk_rule_table, purpose: "Compliance", row_id: "current" },
}

