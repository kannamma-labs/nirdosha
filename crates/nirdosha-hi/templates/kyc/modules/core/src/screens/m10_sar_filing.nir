//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    SARFilingRow, sar_filing_table, s_a_r_filing_store,
};

nirdosha_rt::crud_screens! {
    mount: mount_sar_filing,
    entity: SARFilingRow,
    store: s_a_r_filing_store,
    path: "/reports/sar",
    fields: [ filing_id: String, case_id: String, filing_status: String, submitted_at: String ],
    create: public,
    read: public,
    update: public,
    delete: public,
    guard: { table: sar_filing_table, purpose: "Compliance", update_fields: [ filing_status: String, submitted_at: String ], create_fields: [ case_id: String, filing_status: String ], },
}

