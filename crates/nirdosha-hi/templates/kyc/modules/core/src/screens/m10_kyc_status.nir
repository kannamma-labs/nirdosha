//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    KYCCaseRow, kyc_case_table,
};

nirdosha_rt::report_builder! {
    mount: mount_kyc_status_report,
    entity: KYCCaseRow,
    table: kyc_case_table,
    path: "/reports/kyc-status",
    title: "KYC Status Report",
    purpose: "Operations",
    access: requires role "kyc_senior_analyst" or role "kyc_mlro" or role "compliance_officer" or role "auditor",
    dimensions: [ status: String, risk_rating: String ],
}

