//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    AuditEntryRow, audit_entry_table, audit_entry_store,
};

nirdosha_rt::crud_screens! {
    mount: mount_audit_log,
    entity: AuditEntryRow,
    store: audit_entry_store,
    path: "/audit/log",
    fields: [ entry_id: String, actor_id: String, action: String, resource: String, occurred_at: String ],
    create: public,
    read: public,
    update: public,
    delete: public,
    guard: { table: audit_entry_table, purpose: "Compliance", },
}

