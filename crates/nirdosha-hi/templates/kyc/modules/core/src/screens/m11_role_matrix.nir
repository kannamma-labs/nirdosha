//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    ChecklistTemplateRow, checklist_template_table, checklist_template_store,
};

nirdosha_rt::settings_screen! {
    mount: mount_role_matrix,
    entity: ChecklistTemplateRow,
    store: checklist_template_store,
    path: "/config/roles",
    fields: [ template_id: String, name: String, applies_to: String, steps: String ],
    access: requires role "platform_admin",
    guard: { table: checklist_template_table, purpose: "Administration", row_id: "singleton" },
}

