//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    PeriodicReviewRow, periodic_review_table,
};

nirdosha_rt::report_builder! {
    mount: mount_monitoring_report,
    entity: PeriodicReviewRow,
    table: periodic_review_table,
    path: "/monitoring/report",
    title: "Monitoring Report",
    purpose: "Operations",
    access: requires role "kyc_mlro" or role "compliance_officer" or role "auditor",
    dimensions: [ cadence: String, review_status: String ],
}

