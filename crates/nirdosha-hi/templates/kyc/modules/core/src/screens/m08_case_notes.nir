//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    CaseNoteRow, case_note_table, case_note_cell,
};

nirdosha_rt::communication_feed! {
    mount: mount_case_notes,
    entity: CaseNoteRow,
    store: case_note_cell,
    path: "/cases/{id}/notes",
    fields: [ note_id: String, case_id: String, author_id: String, body: Option<String>, created_at: String ],
    post_access: requires role "kyc_analyst" or role "kyc_senior_analyst" or role "kyc_mlro",
    read_access: requires role "kyc_analyst" or role "kyc_senior_analyst" or role "kyc_mlro",
    refresh_seconds: 5,
    guard: { table: case_note_table, purpose: "Operations" },
}

