//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    screening_hit_table,
};

nirdosha_rt::approval_inbox! {
    mount: mount_screening_hit_review,
    path: "/screening/hits",
    access: requires role "kyc_senior_analyst" or role "kyc_mlro" or role "compliance_officer",
    sources: [
        { table: screening_hit_table, chain: "screening_hit_review", resource: "screeninghit", detail_path: "/screening/hits/{id}", purpose: "Compliance" },
    ],
}

