//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    WatchlistEntryRow, watchlist_table, watchlist_entry_store,
};

nirdosha_rt::crud_screens! {
    mount: mount_watchlist_management,
    entity: WatchlistEntryRow,
    store: watchlist_entry_store,
    path: "/screening/watchlists",
    fields: [ entry_id: String, list_name: String, subject_name: String, reason: String, source_ref: String ],
    create: public,
    read: public,
    update: public,
    delete: public,
    guard: { table: watchlist_table, purpose: "Compliance", update_fields: [ list_name: String, subject_name: String, reason: String ], create_fields: [ list_name: String, subject_name: String, reason: String, source_ref: String ], },
}

