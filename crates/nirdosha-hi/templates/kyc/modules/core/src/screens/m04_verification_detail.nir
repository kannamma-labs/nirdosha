//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    document_table,
    VerificationRunRow, verification_run_table,
};

pub fn __panel_signals(auth: &nirdosha_rt::Auth, subject_id: &str) -> Result<Vec<serde_json::Value>, String> {
    let rows = document_table().guarded_snapshot(auth, "Operations").map_err(|e| e.to_string())?;
    let mut out: Vec<serde_json::Value> = Vec::new();
    for row in rows {
        if row.customer_id == *subject_id {
            out.push(serde_json::to_value(&row).map_err(|e| e.to_string())?);
        }
    }
    Ok(out)
}

pub fn verification_run_workspace_label(row: &VerificationRunRow) -> Vec<(String, String)> {
    vec![
        ("VerificationRun".to_string(), row.run_id.clone()),
    ]
}

nirdosha_rt::workspace! {
    mount: mount_verification_detail,
    path: "/verifications/{id}",
    subject: { table: verification_run_table, purpose: "Operations", label_fn: verification_run_workspace_label },
    budget: { max_rows: 500, max_execution_ms: 2000 },
    panels: [
        { need: "signals", title: "Signals", render: "list", source: __panel_signals },
    ],
}

