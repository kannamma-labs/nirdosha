//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    UBORecordRow, ubo_record_table,
};

nirdosha_rt::tree_view! {
    mount: mount_ubo_tree,
    entity: UBORecordRow,
    table: ubo_record_table,
    path: "/corporate/entities/{id}/ubo",
    title: "UBO Tree",
    purpose: "Compliance",
    access: requires role "kyc_analyst" or role "kyc_senior_analyst" or role "compliance_officer" or role "auditor",
    id_field: ubo_id,
    parent_field: parent_id,
    label_field: label,
}

