//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    DocumentRow, document_table, document_store,
};

nirdosha_rt::crud_screens! {
    mount: mount_document_upload,
    entity: DocumentRow,
    store: document_store,
    path: "/documents/upload",
    fields: [ document_id: String, customer_id: String, document_type: String, content_sha256: String, issued_country: String ],
    create: public,
    read: public,
    update: public,
    delete: public,
    guard: { table: document_table, purpose: "Operations", update_fields: [ document_type: String, issued_country: String ], create_fields: [ customer_id: String, document_type: String, content_sha256: String, issued_country: String ], },
}

