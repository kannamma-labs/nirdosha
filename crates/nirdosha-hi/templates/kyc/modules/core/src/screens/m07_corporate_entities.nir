//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    CorporateEntityRow, corporate_entity_table, corporate_entity_store,
};

nirdosha_rt::crud_screens! {
    mount: mount_corporate_entities,
    entity: CorporateEntityRow,
    store: corporate_entity_store,
    path: "/corporate/entities",
    fields: [ entity_id: String, legal_name: String, registration_number: String, jurisdiction: String, entity_type: String ],
    create: public,
    read: public,
    update: public,
    delete: public,
    guard: { table: corporate_entity_table, purpose: "Operations", update_fields: [ legal_name: String, jurisdiction: String, entity_type: String ], create_fields: [ legal_name: String, registration_number: String, jurisdiction: String, entity_type: String ], },
}

