//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    ScreeningRunRow, screening_run_table, screening_run_store,
};

nirdosha_rt::crud_screens! {
    mount: mount_screening_runs,
    entity: ScreeningRunRow,
    store: screening_run_store,
    path: "/screening/runs",
    fields: [ run_id: String, customer_id: String, run_type: String, hit_count: String, run_at: String ],
    create: public,
    read: public,
    update: public,
    delete: public,
    guard: { table: screening_run_table, purpose: "Compliance", create_fields: [ customer_id: String, run_type: String ], },
}

