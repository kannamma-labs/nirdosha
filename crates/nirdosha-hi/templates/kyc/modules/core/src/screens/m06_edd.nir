//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    EDDQuestionnaireRow, edd_questionnaire_table, e_d_d_questionnaire_store,
};

nirdosha_rt::wizard! {
    mount: mount_edd_questionnaire,
    entity: EDDQuestionnaireRow,
    store: e_d_d_questionnaire_store,
    path: "/edd/{customer_id}",
    access: requires role "kyc_analyst" or role "kyc_senior_analyst" or role "relationship_manager",
    steps: [
        { name: "", fields: [  ] },
        { name: "", fields: [  ] },
        { name: "", fields: [  ] },
        { name: "", fields: [  ] },
        { name: "", fields: [  ] },
    ],
    guard: { table: edd_questionnaire_table, purpose: "Compliance" },
}

