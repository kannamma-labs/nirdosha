//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    KYCCaseRow, kyc_case_table, k_y_c_case_store,
};

nirdosha_rt::kanban_board! {
    mount: mount_case_pipeline,
    entity: KYCCaseRow,
    store: k_y_c_case_store,
    path: "/cases/pipeline",
    access: requires role "kyc_senior_analyst" or role "kyc_mlro",
    title_field: case_id,
    column_field: status,
    columns: [  ],
    move_path: "/cases/pipeline/{id}/move/{to}",
}

pub fn mount_case_pipeline_move(router: nirdosha_rt::Router) -> nirdosha_rt::Router {
    router.post_with_auth("/cases/pipeline/{id}/move/{to}", "Move card", |_req, params, auth| {
        let Some(id) = params.get("id") else { return nirdosha_rt::Response::bad_request("id required") };
        let Some(to) = params.get("to") else { return nirdosha_rt::Response::bad_request("to required") };
        let changed: std::collections::HashSet<String> = ["status"].into_iter().map(|s| s.to_string()).collect();
        match kyc_case_table().guarded_update(auth, "Operations", id, &changed, move |e| { e.status = to.to_string(); }) {
            Ok(_entity) => nirdosha_rt::Response::redirect("/cases/pipeline"),
            Err(e) => nirdosha_guard_screens::guard_error_response(e),
        }
    })
}

