//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    PeriodicReviewRow, periodic_review_table, periodic_review_store,
};

nirdosha_rt::crud_screens! {
    mount: mount_periodic_reviews,
    entity: PeriodicReviewRow,
    store: periodic_review_store,
    path: "/monitoring/reviews",
    fields: [ review_id: String, customer_id: String, due_at: String, review_status: String, cadence: String ],
    create: public,
    read: public,
    update: public,
    delete: public,
    guard: { table: periodic_review_table, purpose: "Operations", update_fields: [ due_at: String, review_status: String, cadence: String ], create_fields: [ customer_id: String, due_at: String, cadence: String ], },
}

