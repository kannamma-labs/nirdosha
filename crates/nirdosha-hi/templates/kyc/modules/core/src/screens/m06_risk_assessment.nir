//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    RiskAssessmentRow, risk_assessment_table,
    risk_rule_table,
};

pub fn __panel_rules(auth: &nirdosha_rt::Auth, subject_id: &str) -> Result<Vec<serde_json::Value>, String> {
    let rows = risk_rule_table().guarded_snapshot(auth, "Compliance").map_err(|e| e.to_string())?;
    let mut out: Vec<serde_json::Value> = Vec::new();
    for row in rows {
        if row.rule_id == *subject_id {
            out.push(serde_json::to_value(&row).map_err(|e| e.to_string())?);
        }
    }
    Ok(out)
}

pub fn risk_assessment_workspace_label(row: &RiskAssessmentRow) -> Vec<(String, String)> {
    vec![
        ("RiskAssessment".to_string(), row.assessment_id.clone()),
    ]
}

nirdosha_rt::workspace! {
    mount: mount_risk_assessment,
    path: "/risk/{customer_id}",
    subject: { table: risk_assessment_table, purpose: "Compliance", label_fn: risk_assessment_workspace_label },
    budget: { max_rows: 500, max_execution_ms: 2000 },
    panels: [
        { need: "rules", title: "Applied rules", render: "list", source: __panel_rules },
    ],
}

