//! Generated by `cargo nirdosha generate-screens` — edit screens.toml, not this file.

// The macro grammar requires `entity:`/`store:` keys even where the
// guard-mode expansion never references them — keep the imports that
// match the invocation text and silence the residue.
#[allow(unused_imports)]
use crate::bridge::{
    DocumentRequirementRow, document_requirement_table, document_requirement_store,
};

nirdosha_rt::crud_screens! {
    mount: mount_document_requirements,
    entity: DocumentRequirementRow,
    store: document_requirement_store,
    path: "/config/document-requirements",
    fields: [ requirement_id: String, document_type: String, customer_type: String, is_mandatory: String ],
    create: public,
    read: public,
    update: public,
    delete: public,
    guard: { table: document_requirement_table, purpose: "Administration", update_fields: [ document_type: String, customer_type: String, is_mandatory: String ], create_fields: [ document_type: String, customer_type: String, is_mandatory: String ], },
}

