# Nirdosha — GBNF grammar for constrained decoding.
#
# Hand-translated from docs/GRAMMAR.md's EBNF, production-for-production, the
# same discipline `crates/grammar_check/src/nirdosha.lalrpop` already follows for
# its own (different-purpose) cross-check. This is **not** a mechanical
# export — there is no tool that turns `parser.rs`'s hand-written
# recursive descent, or docs/GRAMMAR.md's EBNF, into GBNF automatically. See
# the unified development plan's §4.2.3, and `../crates/grammar_export/README.md`
# for the full story of what's actually been checked and how (a real
# llama.cpp grammar-parser dependency, plus a fidelity corpus comparing
# accept/reject behavior against the real lexer+parser).
#
# 2026-08-25: brought current with docs/GRAMMAR.md after `tests/fidelity.rs`
# caught real drift — this file previously only covered `fn_decl` plus
# the base expression/statement language (a v1-era snapshot). It now
# also covers `struct`/`enum`/`match` (row 11), `effect(...)`/
# `requires(...)` annotations (row 12), `.ident` field access, generic
# type application (`Pair(i64, str)`), `transact`, the `screen`/
# `dashboard`/`module`/`workflow` UI/workflow DSLs, `audited`, and
# `listen`/`accept` — every production docs/GRAMMAR.md documents.
#
# 2026-09-05: brought current again after `tests/fidelity.rs` was fixed to
# actually recurse into `examples/`'s subdirectories (it previously swept
# a flat directory and had silently stopped checking anything once real
# examples moved into `examples/syntax/`/`examples/features/`) and caught
# real drift a second time: `validate <fn> { pre:/post: }` (row F3) was
# entirely unmodeled (`item` had no alternative for it at all); `module
# Ident { pub ... }`'s real-namespace form and top-of-file `use "path"`
# (row F2) existed only as the legacy string-named nav-grouping form;
# `Mod::Name` qualified references (`qualified-ident`, row F2) were
# unrepresented everywhere `ident` names something to look up rather
# than declare (`type`, `primary`, `variant-arm`); a `workflow` `state`'s
# `owner: role(...)` kv-entry had no room in `state-decl`'s old fixed
# `on_entry?/on_exit?/transition*` order; and `layout { row/column/grid/
# group/tabs/divider/... }` (row F4) didn't exist at all. Also fixed in
# passing, found by the same real-parser check, not by inspection:
# `dashboard-item`'s two-line body had no enclosing `(...)` around its
# `|` — the exact "multi-line body not inside one already-open group"
# trap this file's own note two paragraphs below already names, just not
# yet applied there when `dashboard-item` was added.
#
# 2026-09-06: brought current after `nfr(...)` (function-level NFR
# tracking, `docs/LANGUAGE.md` §6f) and field-level `requires(role/claim:
# ...)` masking (§6e) were added to `fn_decl`/`field` in docs/GRAMMAR.md --
# `nfr-annotation`/`nfr-field` and `field-mask-requires` here mirror those
# new productions production-for-production, same as every other entry
# in this changelog. `tests/fidelity.rs`'s
# `every_shipped_example_is_accepted_by_both...` caught a second, real,
# pre-existing drift while re-running it for this change (not introduced
# by it): `froze`/`*froze` (docs/GRAMMAR.md's own `type`/`unary`
# productions have had it since the Eighteenth `docs/PHASE0.md` update)
# was entirely missing from `type`/`unary` here, rejecting
# `examples/syntax/04_ownership_and_concurrency.nir` outright -- fixed
# alongside, same "found by the real check, not by re-reading this file"
# pattern as everything else in this changelog.
#
# Found by that real check, not by re-reading this file more carefully:
# llama.cpp's GBNF parser requires a rule's *entire* multi-line body to
# sit inside one already-open, unclosed group — not just multi-line
# *alternation* (`type`, `scalar-type-name`, `unary`, `primary` all wrap
# in `(...)` for exactly that reason). A multi-line rule with no `|` at
# all still needs the same wrapping (found via `transact-expr`: without
# it, the real parser reports "expecting name" at the second line,
# because once a line ends with no group left open, it starts looking
# for the next rule's `name ::=`, not a continuation of this one).
# Nothing in GBNF's documentation makes either of these obvious in
# advance — both found by running the real llama.cpp grammar parser
# against this file, not by re-reading the file more carefully.
#
# Known, deliberate imprecision: `ident` accepts any identifier-shaped
# text, including this language's keywords (`let`, `fn`, `if`, ...) —
# GBNF has no clean negative-lookahead/"not a keyword" primitive without
# enumerating every keyword as an explicit exclusion, which would roughly
# double this file's size for a property that costs nothing to get wrong
# in the direction this does: a constrained decoder that occasionally
# permits `let` as a bare identifier where the real grammar wouldn't is
# *safe* (the real parser/typeck still rejects it — same "over-permissive
# in a safe direction" tradeoff `docs/GRAMMAR.md`'s own LALR(1) cross-check
# documents for the statement-separator ambiguity), whereas a decoder
# that's ever *too strict* would block a legal completion outright. The
# fidelity corpus (`tests/gbnf.rs`) tests real programs, not adversarial
# keyword-as-identifier cases, so this gap doesn't show up there. Every
# keyword-literal alternative below is listed before the generic
# `ident`/`call` fallback in the same production (`type`'s scalar names
# before its trailing `ident(...)` case, `unary`'s `spawn`/`sandbox`/...
# before its trailing `call` case) — the same order-of-alternatives
# discipline the pre-existing `unary` rule already relied on, now applied
# consistently to every rule added in the 2026-08-25 update too.

root        ::= ws program ws

program     ::= (use-decl ws)* (item ws)*

# `use "relative/path.nir"` -- only legal in this leading run, before
# any other item; a `use` anywhere else falls through to `item`'s own
# dispatch and is an ordinary "expected an item" rejection, not
# special-cased here either.
use-decl    ::= "use" ws str-lit

item        ::= fn-decl | struct-decl | enum-decl | screen-decl | dashboard-decl | module-decl | workflow-decl | workspace-decl | validate-decl

# `Mod::Name` / `Mod::Enum::Variant` -- a qualified reference, never a
# declaration site (every `_-decl`'s own name stays a plain `ident`).
# Reused wherever `ident` names something to look up rather than
# declare: `type`'s struct/enum alternative, `primary`'s bare-name
# alternative, and `variant-arm`'s pattern.
qualified-ident ::= ident (ws "::" ws ident)*

fn-decl     ::= "fn" ws ident ws "(" ws params? ws ")" ws ret-type? (ws effect-annotation)? (ws requires-annotation)? (ws nfr-annotation)? ws block

effect-annotation ::= "effect" ws "(" ws effect-name (ws "," ws effect-name)* ws ")"
effect-name ::= "pure" | "rng" | "io" | "concurrent" | "network"

requires-annotation ::= "requires" ws "(" ws (
                           ("role" ws ":" ws str-lit)
                         | ("claim" ws ":" ws str-lit ws "," ws str-lit)
                         | "public"
                       ) ws ")"

# `nfr(latency_ms: 200, error_rate_max: 0.01, throughput_min_per_sec: 50,
# concurrency_max: 100)` -- all four fields optional, any order, each
# name matched at most once (not enforced by this grammar, same as
# `requires-annotation`'s own field-name slot). No trailing comma
# (unlike `field`/`variant` below) -- `parser.rs::parse_nfr_annotation`
# has no trailing-comma branch.
nfr-annotation ::= "nfr" ws "(" ws nfr-field (ws "," ws nfr-field)* ws ")"
nfr-field ::= (
                ("latency_ms" ws ":" ws int-lit)
              | ("error_rate_max" ws ":" ws (float-lit | int-lit))
              | ("throughput_min_per_sec" ws ":" ws int-lit)
              | ("concurrency_max" ws ":" ws int-lit)
              )

struct-decl ::= "struct" ws ident (ws type-param-list)? ws "{" ws field (ws "," ws field)* (ws ",")? ws "}"
field       ::= ident ws ":" ws type (ws field-mask-requires)?
type-param-list ::= "(" ws ident (ws "," ws ident)* ws ")"

# `salary: f64 requires(role: "admin")` -- field-level masking
# (`docs/LANGUAGE.md` §6e). Same `role`/`claim` shape as
# `requires-annotation` above, minus the `public` alternative (field
# masking has no gate to opt out of).
field-mask-requires ::= "requires" ws "(" ws (
                           ("role" ws ":" ws str-lit)
                         | ("claim" ws ":" ws str-lit ws "," ws str-lit)
                       ) ws ")"

enum-decl   ::= "enum" ws ident (ws type-param-list)? ws "{" ws variant (ws "," ws variant)* (ws ",")? ws "}"
variant     ::= ident (ws "(" ws type (ws "," ws type)* ws ")")?

screen-decl    ::= "screen" ws ident ws "{" ws (screen-item ws)* "}"
screen-item    ::= paginate-block | field-override | action-decl | layout-decl | kv-entry
paginate-block ::= "paginate" ws "{" ws (kv-entry ws)* "}"
field-override ::= "field" ws ident ws "{" ws (kv-entry ws)* "}"
action-decl    ::= "action" ws str-lit ws "->" ws ident (ws "{" ws (kv-entry ws)* "}")?
kv-entry       ::= ident ws ":" ws expr

# `layout { row { column { group "Title" { field x } } } }` -- an
# optional arrangement tree over a screen's own field/action set.
# `field`/`action` here *reference* an existing `screen-item`'s field/
# action by name, never redeclare one -- typeck's job to check the name
# resolves, not this grammar's.
layout-decl ::= "layout" ws "{" ws (layout-node ws)* "}"
layout-node ::= (
                  (("row" | "column" | "grid" | ("group" (ws str-lit)?)) ws layout-body)
                | ("tabs" ws "{" ws ("tab" ws str-lit ws "{" ws (layout-node ws)* "}" ws)* "}")
                | ("field" ws ident)
                | ("action" ws str-lit)
                | (ident ws layout-body)
                )
layout-body ::= "{" ws (kv-entry ws)* (layout-node ws)* "}"

dashboard-decl ::= "dashboard" ws "{" ws (dashboard-item ws)* "}"
# Wrapped in one outer `(...)` even though it's already an alternation
# -- llama.cpp's real GBNF parser requires a rule's entire multi-line
# body to sit inside one already-open, unclosed group (this file's own
# header comment), not just a bare `|` continuing onto the next line;
# without the wrap it reports "expecting name" at the second line,
# having already closed the rule at the first line's balanced parens.
dashboard-item ::= (
                     (("tile" | "chart") ws str-lit ws "->" ws ident)
                   | ("visual" ws str-lit ws "->" ws ident (ws "{" ws (kv-entry ws)* "}")?)
                   )

# `workspace Name { subject: Struct panel "..." { ... } }` -- Track E1's
# composite multi-panel screen. `action-decl` inside `panel-item` is
# `screen-item`'s own production, reused unchanged.
workspace-decl ::= "workspace" ws ident ws "{" ws (workspace-item ws)* "}"
workspace-item ::= panel-decl | kv-entry
panel-decl     ::= "panel" ws str-lit ws "{" ws (panel-item ws)* "}"
panel-item     ::= action-decl | kv-entry

# Two unrelated forms, told apart by the very next token after `module`
# (a string literal -- legacy nav grouping, pure sugar, no scoping -- or
# a bare `ident` -- a real namespace, `pub`-gated). Wrapped in one outer
# `(...)` per this file's own multi-line-body-needs-wrapping discipline.
module-decl ::= (
                  ("module" ws str-lit ws "{" ws ((fn-decl | struct-decl | enum-decl) ws)* "}")
                | ("module" ws ident ws "{" ws ("nav" ws ":" ws str-lit ws)? (pub-item ws)* "}")
                )
pub-item    ::= ("pub" ws)? (fn-decl | struct-decl | enum-decl)

# `validate <fn_name> { pre: <expr>  post: <expr> ... }` -- a Hoare
# contract on an existing `fn`, declared separately. `pre`/`post` are
# ordinary `kv-entry`s (this file already has no separate predicate
# grammar to add).
validate-decl ::= "validate" ws ident ws "{" ws (kv-entry ws)* "}"

workflow-decl  ::= "workflow" ws ident ws "{" ws (data-block ws)? (state-decl ws)+ "}"
data-block     ::= "data" ws "{" ws field (ws "," ws field)* (ws ",")? ws "}"
# `state-item*` is unordered and repeatable (unlike an earlier, stricter
# fixed-order draft of this rule) -- a real state can carry an `owner:
# role(...)` kv-entry alongside any number of on_entry/on_exit/
# transition items, in any order, the same "no arbitrary shape assumed"
# posture kv-entry's own fallback already has elsewhere in this file.
state-decl     ::= "state" ws ident (ws "terminal")? ws "{" ws (state-item ws)* "}"
state-item     ::= on-entry-block | on-exit-block | transition | kv-entry
on-entry-block ::= "on_entry" ws "{" ws (action-call ws)* "}"
on-exit-block  ::= "on_exit" ws "{" ws (action-call ws)* "}"
action-call    ::= ident ws "(" ws (expr (ws "," ws expr)*)? ws ")"
transition     ::= "on" (ws "link")? ws ident ws "->" ws ident

params      ::= param (ws "," ws param)*
param       ::= ident ws ":" ws type

ret-type    ::= "->" ws type

type        ::= (
                  "&" ws type
                | "box" ws type
                | "froze" ws type
                | "thread" ws type
                | "chan" ws type
                | "sandbox"
                | "Vector" ws "(" ws type ws "," ws int-lit ws ")"
                | "Matrix" ws "(" ws type ws "," ws int-lit ws "," ws int-lit ws ")"
                | scalar-type-name
                | qualified-ident (ws "(" ws type (ws "," ws type)* ws ")")?
                | "fn" ws "(" ws (type (ws "," ws type)*)? ws ")" (ws "->" ws type)?
              )

scalar-type-name ::= (
                    "i8" | "i16" | "i32" | "i64"
                  | "u8" | "u16" | "u32" | "u64" | "usize" | "f64"
                  | "bool" | "unit" | "str" | "tcp_listener" | "tcp"
                  )

block       ::= "{" ws stmt* ws "}"

stmt        ::= (let-stmt | return-stmt | while-stmt | audited-stmt | expr-stmt) ws

let-stmt    ::= "let" ws ident ws ":" ws type ws "=" ws expr
return-stmt ::= "return" (ws expr)?
while-stmt  ::= "while" ws expr ws block
audited-stmt ::= "audited" ws str-lit ws "{" ws stmt* ws "}"
expr-stmt   ::= expr

expr        ::= if-expr | transact-expr | match-expr | assignment

if-expr     ::= "if" ws expr ws block (ws "else" ws (block | if-expr))?

match-expr  ::= "match" ws expr ws "{" ws match-arm (ws "," ws match-arm)* (ws ",")? ws "}"
match-arm   ::= variant-arm | literal-arm
variant-arm ::= qualified-ident (ws "(" ws (ident (ws "," ws ident)*)? ws ")")? ws "=>" ws expr
literal-arm ::= (str-lit | int-lit | "true" | "false" | "_") ws "=>" ws expr

transact-expr ::= (
                     "transact" ws "{" ws
                     ("precheck" ws ":" ws call ws)?
                     "network" ws ":" ws call (ws "retry" ws int-lit)? (ws "timeout" ws int-lit)? ws
                     "verify" ws ":" ws call ws
                     "commit" ws ":" ws call
                     (ws "compensate" ws ":" ws call)?
                     (ws "log" ws ":" ws call)?
                     ws "}"
                   )

assignment  ::= (ident ws "=" ws assignment) | logic-or

logic-or    ::= logic-and (ws "||" ws logic-and)*
logic-and   ::= equality (ws "&&" ws equality)*
equality    ::= comparison (ws ("==" | "!=") ws comparison)*
comparison  ::= additive (ws ("<=" | ">=" | "<" | ">") ws additive)*
additive    ::= multiplicative (ws ("+" | "-") ws multiplicative)*
multiplicative ::= unary (ws (".*" | "./" | "*" | "/") ws unary)*

unary       ::= (
                  ("!" | "-" | "*" | "box" | "froze" | "&") ws unary
                | "spawn" ws call
                | "join" ws unary
                | "chan"
                | "send" ws "(" ws expr ws "," ws expr ws ")"
                | "recv" ws "(" ws expr ws ")"
                | "sandbox" ws call
                | "stop" ws unary
                | "connect" ws "(" ws expr ws "," ws expr ws ")"
                | "listen" ws "(" ws expr ws ")"
                | "accept" ws "(" ws expr ws ")"
                | "acquire" ws call
                | call
              )

call        ::= postfix (ws "(" ws args? ws ")")?
args        ::= expr (ws "," ws expr)*

postfix     ::= primary (ws (("[" ws expr (ws "," ws expr)* ws "]") | ("." ws ident)))*

primary     ::= (
                  float-lit | int-lit | str-lit | "true" | "false" | qualified-ident
                | "(" ws expr ws ")"
                | array-lit
              )

array-lit   ::= "[" ws expr (ws "," ws expr)* ws "]"

str-lit     ::= "\"" str-char* "\""
str-char    ::= [^"\\] | "\\" ["\\ntr]

int-lit     ::= [0-9]+
float-lit   ::= [0-9]+ "." [0-9]+

ident       ::= [a-zA-Z_] [a-zA-Z0-9_]*

ws          ::= ( [ \t\r\n] | "//" [^\n]* "\n" )*
