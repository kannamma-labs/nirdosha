[package]
name = "nirdosha-guard-core"
version = "0.1.0"
edition = "2021"
authors = ["Nirdosha Contributors"]
description = "Store-agnostic access-control IR and invariants for Nirdosha Guard"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
